/**
 * Generated file. Do not edit by hand.
 *
 * `npm run build:manifest` reads every JSON file in `metadata/` and writes them
 * here, in filename order, so a single deploy carries your whole metadata set
 * and the server needs no external storage.
 *
 * After you run it, this file contains the artwork nobody is supposed to see
 * yet. Keep your copy of this repository private, or switch
 * `metadata.source` to "r2" so the metadata never touches git.
 */

import type { TokenMetadata } from "../config.ts";

export const MANIFEST: TokenMetadata[] = [];

/** SHA-256 over the manifest, published as part of your provenance record. */
export const MANIFEST_HASH: string = "";

/** When `npm run build:manifest` last ran. */
export const MANIFEST_BUILT_AT: string = "";
